services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_DB: jokes
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./backend/schema.sql:/docker-entrypoint-initdb.d/schema.sql
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    image: ghcr.io/bcp-schulung/container-15.07.2026/joke-backend:latest
    environment:
      DB_HOST: postgres
      DB_PORT: 5432
      DB_NAME: jokes
      DB_USER: postgres
      DB_PASSWORD: postgres
    ports:
      - "3000:3000"
    depends_on:
      postgres:
        condition: service_healthy

  frontend:
    image: ghcr.io/bcp-schulung/container-15.07.2026/joke-frontend:latest
    environment:
      BACKEND_URL: http://localhost:3000
      PORT: 8080
    ports:
      - "8080:8080"
    depends_on:
      - backend

volumes:
  postgres_data:
